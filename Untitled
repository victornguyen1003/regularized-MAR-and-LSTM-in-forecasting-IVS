I need you to write Python code (using numpy) to estimate the Matrix Autoregressive, MAR(1), coefficient matrices A (shape m x m) and B (shape n x n).

Please implement the Projection Method (PROJ) and the Iterated Least Squares Method (LSE) exactly as described in the methodology below.

Method 1: Projection Method (PROJ)
The goal is to find the nearest Kronecker product to Phi_hat.

Define a rearrangement operator G that maps a matrix C (in R^{mn x mn}) to a matrix (in R^{m^2 x n^2}) such that G(B kronecker A) = vec(A) * vec(B).T

Rearrange my VAR coefficient matrix: Phi_tilde = G(Phi_hat).

Perform a Singular Value Decomposition (SVD) on Phi_tilde.

Extract the largest singular value d_1, and its corresponding first left and right singular vectors u_1 and v_1.

Calculate vec(A_hat_1) * vec(B_hat_1).T = d_1 * u_1 * v_1.T

Convert the vectors back into matrices A_hat_1 and B_hat_1. Normalize A_hat_1 so its Frobenius norm is exactly 1.

Method 2: Iterated Least Squares (LSE)
This method minimizes the sum of squared Frobenius norms: Sum over t of ||X_t - A * X_{t-1} * B.T||_F^2

Use the matrices A_hat_1 and B_hat_1 from the PROJ method as your starting/initial values.

Iteratively update B while holding A fixed using this equation:
B = (Sum[X_t.T * A * X_{t-1}]) @ Inverse(Sum[X_{t-1}.T * A.T * A * X_{t-1}])

Iteratively update A while holding B fixed using this equation:
A = (Sum[X_t * B * X_{t-1}.T]) @ Inverse(Sum[X_{t-1} * B.T * B * X_{t-1}.T])

After each full iteration (or at convergence), enforce the identification convention by scaling the matrices so that the Frobenius norm of A = 1 (and adjust B accordingly).

Continue iterating until the changes in A and B fall below a small tolerance threshold (e.g., 1e-6).